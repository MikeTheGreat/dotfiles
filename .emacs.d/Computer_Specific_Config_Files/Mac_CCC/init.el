					; contains info that will change between different physical computers
; The goal is to allow for different directory structures on different computers

(setq DROPBOX_ROOT "/Users/mikepanitz/Dropbox")
(message (concat "\tDropbox root: " DROPBOX_ROOT))

(setq STUDENT_WORK_ROOT "/Users/mikepanitz/Desktop/Student_Work")
(message (concat "\tWork root: " STUDENT_WORK_ROOT))

